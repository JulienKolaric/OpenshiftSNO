apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: kvm-emulation-patch.yaml

labels:
  - includeSelectors: false
    pairs:
      environment: dev
